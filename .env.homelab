# Homelab environment (committed; no secrets).
# Used to document the server-side runtime env for the homelab deployment.

# Environment selector (server-side, runtime). homelab -> info-level logging.
APP_ENV=homelab
# Optional explicit override:
# LOG_LEVEL=info
